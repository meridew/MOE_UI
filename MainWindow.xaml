<Window x:Class="MOE_UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:MOE_UI.Controls"
        mc:Ignorable="d"
        DataContext="{StaticResource Locator}" d:DesignHeight="768" d:DesignWidth="1024">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <controls:ControlGrid/>
        <controls:EmailAddresses Grid.Row="1" Grid.RowSpan="3"/>
        <controls:Regions Grid.Column="1"/>
        <controls:Criteria Grid.Row="1" Grid.Column="1"/>
        <controls:Stages Grid.Row="2" Grid.Column="1"/>
        <Button Grid.Row="3" Grid.Column="1" Content="Create Campaign" Command="{Binding RunCommand}"/>
    </Grid>
</Window>
